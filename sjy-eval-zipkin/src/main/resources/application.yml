spring:
  application:
      name: eval-zipkin
  ##表示当前程序不使用sleuth
  sleuth:
    enabled: false
    sampler:
      percentage: 1
eureka:
  client:
    service-url:
      defaultZone: http://admin:eva321@localhost:8761/eureka/
server:
    port: 7777 #启动端口
