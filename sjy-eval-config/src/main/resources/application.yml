spring:
  application:
    name: eval-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yxbook/sjy-eval-config.git
          username: yxbook@live.cn
          password: youxun123
          basedir: F:\basedir
eureka:
  client:
    service-url:
      defaultZone: http://admin:eva321@localhost:8761/eureka/
server:
    port: 9002 #启动端口

